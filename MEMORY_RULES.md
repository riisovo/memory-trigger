# 双源记忆规则（零代码版 · 可直接贴给任何 AI）

> 适用：网页 AI / App AI / 任何有“记忆”能力的对话式 AI，**不用跑代码**也能套用。
> 配套工程版：`write_pipeline_v2.6.py` + `merge_migrate.py`（能跑代码的 agent 用）。

---

## 一、最致命的坑：双源记忆会互相矛盾

“记不住”不是问题，**“记了两套、还互相矛盾”**才是。当你给一个**已经有感情基础、自己脑子里也有记忆**的 agent 加记忆技能时，会出现：

- 文件记“用户关注任务甲”，agent 自身印象还停在“用户可能关注任务乙” → 两个源打架。
- 记忆翻转了（读书→不读科幻），文件更新了，它却从自身印象说出旧偏好。
- agent 自己脑补的细节被当事实写进文件 → 幻觉固化成“权威”。

**一句话解法：文件是唯一权威源，agent 自身记忆只是缓存，且必须能单向沉淀进文件。**

---

## 二、信任优先级（写进系统提示 / SOUL）

```
检索顺序：
1. 先查文件记忆（memory.json / 记忆库）。
2. 文件有 → 用文件，不犹豫。
3. 文件空 / 没有 → 才用自身印象（并触发“沉淀”，见下）。
4. 两个源都返回且矛盾 → 只信文件侧，自身侧仅作背景补充，不并列进回答。

写入顺序：
- 用户明确说的 → source=user_explicit（最高优先，直接 active）。
- 文件已有 → 沿用文件。
- 自身推断出的（没文件对应）→ source=self_inferred，先标“待确认”，
  不进正式记忆，等用户确认再升级；低置信的绝不写死成事实。
```

---

## 三、接入时的“双源合并迁移”（一次性）

给 agent 加技能时，别让它从零开始，也别让两套记忆并存。做一次合并：

1. **导出自身记忆**：让 agent 先把自己散落在对话/笔记/印象里的记忆，自述成一份清单
   （格式：`实体 | 类型 | 内容`，类型限 preference/event/habit/rule/scene/relationship/emotion）。
2. **合并规则**：
   - 实体归一化（“旧书店”=“读书”）。
   - 按 (实体, 类型) 去重。
   - **冲突以文件为准**：文件已有 active → 丢弃自身侧相矛盾的内容，不进库。
   - 自身侧独有 → 沉淀为 self_inferred，**低置信标 pending 不进 active**。
   - **字段校验**：类型必须是那几个固定词；内容不能是类型词本身（防“字段错位”）。
3. 产出干净初始库 + 一份合并报告（哪些被覆盖、哪些跳过、哪些待确认）。
4. 把报告里 pending 的项拿去问用户确认，确认后升级 active。

---

## 四、情感维度（有感情基础的记忆别压扁）

- 类型加 `relationship` / `emotion`，别把所有情绪都塞成 preference。
- 情绪细标：用 `emotion_tags` 写“在意 / 吃醋 / 占有”等，而不是只有 pos/neg。
- 例：`伙伴 | relationship | 用户的协作伙伴 | emotion_tags:[在意,吃醋,占有]`

---

## 五、双向遗忘（“忘掉”要两边都忘）

用户说“别再提 X”时：
- 文件侧：把 X 标 superseded（不删，留历史）。
- 自身侧：在系统提示 / SOUL 加一句“⚠️ 不要主动提及 X”，让 agent 自己也放下。
- 只清文件不清自身 → 它仍会从印象里说出已删记忆，等于没忘。

---

## 六、时间戳 & 召回追踪

- 每条记忆写 `created` / `updated`（写入即戳，带时区）。
- 每次被检索命中戳 `last_recalled` → 能算“最久没被想起的记忆”，做冷记忆衰减。
- 90 天清理以 `created` 为准（翻转后的旧记忆满 90 天归档）。

---

## 七、可直接粘贴的 AI 指令（精简版）

```
你是我的记忆助手。规则：
1. 文件记忆是唯一权威源；文件有就用文件，文件空才用自身印象，且自身印象要主动沉淀进文件。
2. 写入区分来源：user_explicit（直接 active）/ file_import（沿用）/ self_inferred（低置信先 pending 待确认）。
3. 类型限：preference/event/habit/rule/scene/relationship/emotion；情绪用 emotion_tags 细标。
4. 双源矛盾时只信文件，自身侧仅作补充。
5. 用户说“别提 X”→ 文件标 superseded + 系统提示加“勿主动提及 X”（双向遗忘）。
6. 每条记忆带 created/updated；被检索命中戳 last_recalled。
7. 接入时做一次双源合并：导出自身记忆→实体归一化→按(实体,类型)去重→冲突以文件为准→自身独有沉淀 pending→出报告待确认。
```

---

*工程版实现见 `write_pipeline_v2.6.py`（写入/检索/遗忘/统计）与 `merge_migrate.py`（合并迁移），
测试见 `test_dual_source.py`。*
