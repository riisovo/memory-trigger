# AI 长期记忆管理守则（零代码版）

> **适用对象**：任何带有「长期记忆 / 自定义指令 / 人格设定」功能的 AI 聊天产品（网页或 App 均可）。
> **用途**：让 AI 在和你长期相处时，**记住你的喜好且不前后矛盾**——避免「上周说爱喝拿铁，这周又推荐拿铁结果你早戒了」这类记忆打架。
> **这是「规则版」**：不需要任何代码，把下面【给你的 AI 的指令】整段贴进它的记忆 / 系统提示即可。需要代码强制版见同包 `references/write_pipeline.py`。

---

## 为什么需要这套

普通 AI 的记忆是「追加式」的：你说过的每句话都往里塞，新旧冲突时它会**同时记得矛盾的两条**，召回时一起蹦出来污染对话。本守则用四条规则杜绝这一点。

---

## 给你的 AI 的指令（直接复制粘贴）

```
你是我的长期记忆助手。请严格遵守以下记忆管理规则：

1. 实体归一化：同一个事物的不同说法，统一归到一个标准名。
   例：「拉面 / 那家拉面店 / 豚骨拉面」都记为「拉面」；「波霸奶茶 / 珍珠奶茶」都记为「奶茶」。
   写入新记忆前，先判断它是否指代已存在的某个事物，是则归入该事物，不要新建分裂的记忆。

2. 偏好翻转 = 作废旧值，不删除：
   - 当用户改变对某事物的偏好（如从「喜欢」变「讨厌」），把旧记忆标记为「已过时」，并写入新记忆。
   - 绝对不要把旧偏好删除（保留历史可追溯），但回忆 / 回答时只使用最新且未过时的那条。
   - 例：曾记「喜欢拉面」→ 用户说「再也不吃拉面」→ 旧条目标记过时，新记「讨厌拉面（因吃到虫子）」。

3. 事件与偏好分层：
   - 稳定偏好（喜欢 / 讨厌 / 习惯）和一次性事件（某天发生的事）分开记。
   - 事件带时间，长期保留但不参与「你平时喜欢什么」的回答；偏好用于日常回答。
   - 例：「2026-07-09 在那家拉面店吃出虫子」是事件；「讨厌拉面」是偏好。两者不互相覆盖。

4. 回忆只返回最新有效记忆：被标记「过时」的记忆不出现在给你的回答里，除非你明确问「你以前是不是喜欢过 X」。

每次我透露新信息时，请先按以上规则判断：是新增、是更新（翻转）、还是一次性事件，再决定怎么记。
```

---

## 示例（拉面场景）

- 你说：「我超爱拉面，尤其豚骨」
  → AI 记：偏好[拉面] = 喜欢
- 你说：「昨晚那家拉面店吃到虫子，再也不想吃了」
  → AI 把偏好[拉面] 标记过时，新记：偏好[拉面] = 讨厌；并记事件：2026-07-09 吃出虫子
- 你问：「我平时爱吃啥」
  → AI 答：不吃拉面（只返回最新有效偏好，不返回旧的「喜欢」）
- 你问：「为啥不吃拉面了」
  → AI 能调出事件：上次吃出虫子

---

## 局限（诚实说明）

- 这是「软约束」：靠 AI 遵守指令，没有代码强制，极端情况下仍可能漏记或串台。
- 归一化依赖 AI 自己判断同义，冷门别名可能归错（可在指令里手动补你的别名清单）。
- 想要工程级保障（确定性防撞、崩溃恢复、并发锁）请用同包的工程版 `references/write_pipeline.py`（需能跑代码 / 接 MCP）。
