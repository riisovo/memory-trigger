# memory-trigger 人肉使用指南（维护工具篇）

> 给**人类操作员**看：怎么手动保养你的记忆库，别让它退化成普通追加文件。
> AI 用的一份见仓库 README「⚠️ 让 AI 主动记」整段（直接粘进 bot 的 SOUL），同一内容也内置为 MCP prompt `remember_guidance`（用 `get_prompt` 拉取）。

---

## 0. 一句话定位

memory-trigger 不是"往文件里 append"那么简单——它是带**遗忘曲线、权重排序、核心钉死、双源信任、崩溃恢复**的结构化长期记忆。
但这些智能**只在使用对应工具时才生效**。如果只调 `write` + `search`，它就等于一个带去重的普通记忆文件，其余 9 个工具全在吃灰。

所以：**维护工具要定期跑，最好排定时任务自动跑。**

---

## 1. 工具全景（19 个 MCP 工具）

| 工具 | 类别 | 一句话 |
|---|---|---|
| `memory_write` | 日常 | 写入/更新一条记忆（upsert，按 entity+kind 去重；可带 context 情感锚点 / expires_at 到期） |
| `memory_search` | 日常 | 关键词检索；命中即触发遗忘衰减 |
| `memory_recall` | 日常 | 主动回忆：挑出"此刻值得想起的"旧记忆（带当时的气氛） |
| `memory_forget` | 日常 | 双向遗忘（标 superseded + 让 AI 也放下） |
| `memory_deny` | 日常 | 否认降权：用户纠正过的事立即降权，2 次转 pending |
| `memory_wellness` | 日常 | 记录心情/睡眠体检 |
| `memory_promise` | 日常 | 承诺建档（AI 答应的事必建档，带 deadline 触发闹钟） |
| `memory_promise_done` | 日常 | 承诺完成划掉 |
| `memory_promise_list` | 日常 | 承诺清单 |
| `memory_promise_check` | **每天/每周** | 主动戳未完成承诺（逾期排最前） |
| `memory_promise_watch_status` | 日常 | 承诺闹钟后台线程状态 + 最近推送（v2.10） |
| `memory_decay` | **每周** | 梦境周期：全库非 core 记忆统一遗忘衰减 |
| `memory_expire_check` | **每周** | 到期记忆：过期移出检索，临期提醒兑现 |
| `memory_vacuum` | **每周** | 归档 >90 天前 superseded 的旧记忆，主库瘦身 |
| `memory_stats` | **每周** | 体检：核心数/均权重/最弱5/最久未提 |
| `memory_selfcheck` | **每周** | 扫缺 entity 的旧脏记录并自愈 |
| `memory_backup` | **每周/大改前** | 备份 memory.json / entity_index.json |
| `memory_recover` | 异常 | 崩溃后从 WAL 重放未提交项 |
| `memory_init` | 部署 | 首次建库 |

日常 8 个由 AI 在对话中自然调用；**每周维护（expire_check/decay/vacuum/stats/selfcheck/backup）必须由定时任务自动跑**，AI 不该每次对话手动调。**`memory_promise_check` 建议每次会话开始自查一次**——未兑现的承诺要主动戳，别让它默默消失。**承诺提醒本体（v2.10）由模板自带闹钟负责**：MCP server 后台线程 / `promise watch` / 会话内注入三通道，不依赖上面这个每周定时（详见 README「⏰ 承诺闹钟」）。

---

## 2. 九个维护工具逐个说

### `memory_decay` —— 梦境周期 / 遗忘曲线
- **作用**：对所有 `active` 且非 `core` 的记忆，统一执行 `importance *= 0.995 ** 距上次召回天数`（半衰期≈138 天）。常聊的话题因 `search` 不断重置 `last_recalled` 而稳在高位；冷掉的话题自然下沉。
- **何时用**：**每周一次**（建议排定时任务）。不要等用户提醒——不跑，记忆就永不淡。
- **CLI**：`python write_pipeline.py decay <REFS_DIR>`
- **注意**：只降权重、不删记忆；`core=true` 的记忆永不衰减。

### `memory_vacuum` —— 归档瘦身
- **作用**：把 `>90` 天前被标 `superseded`（已遗忘/过期）的记忆移到 `.archive/memory_YYYYMMDD.json`，主库只留活跃记忆。
- **何时用**：**每周一次**，紧跟 decay 之后。
- **CLI**：`python write_pipeline.py vacuum <REFS_DIR>`

### `memory_stats` —— 健康体检
- **作用**：返回 `core_memories`（核心数）、`avg_importance_noncore`（非核平均权重）、`weakest_active`（最弱 5 条）、`most_stale_active`（最久未召回 5 条）、各类计数。
- **何时用**：**每周一次**，结果应推给主人看（如 Bark 周报）。想知道"TA 现在记得多少"随时也能手动跑。
- **CLI**：`python write_pipeline.py stats <REFS_DIR>`

### `memory_selfcheck` —— 脏数据自愈
- **作用**：扫描早期手写入库、缺 `entity` 字段的旧记录，按内容首行派生实体名自动补全。
- **何时用**：并入**每周维护**顺手跑；或发现检索异常时手动跑。
- **CLI**：`python write_pipeline.py selfcheck <REFS_DIR>`

### `memory_backup` —— 备份
- **作用**：把 `memory.json` / `entity_index.json` 复制到 `.backup/`（保留最近 10 组，按微秒时间戳避免同秒覆盖）。
- **何时用**：**每周维护开头**兜底一份；大改记忆库前也先跑。正常写入已自动 write-through 备份，这步是双保险。
- **CLI**：`python write_pipeline.py backup <REFS_DIR>`

### `memory_forget` —— 双向遗忘
- **作用**：把记忆标 `superseded`，并写 `.suppressed.json` + 生成 `suppressed_prompt.md`，让 AI 自身也"放下"（不再主动提）。
- **何时用**：用户说"别提了 / 忘了 X"时。可指定 `--kind` / `--reason`。
- **CLI**：`python write_pipeline.py forget "<entity或id>" <REFS_DIR> --reason "用户要求"`

### `memory_recover` —— 崩溃恢复
- **作用**：从 `.wal.jsonl` 重放崩溃时未提交的 upsert/commit。
- **何时用**：疑似写入中断、记忆"少了"时手动跑一次。
- **CLI**：`python write_pipeline.py recover <REFS_DIR>`

### `memory_wellness` —— 心情体检
- **作用**：追加一条心情/睡眠记录到 `wellness.json`。
- **何时用**：用户表达状态时由 AI 调；也可手动 `python write_pipeline.py wellness 开心 <REFS_DIR>`。

### `memory_recall` —— 主动回忆（v2.9）
- **作用**：从记忆库挑出"此刻值得想起的"几条。**双通道**：常聊/核心记忆按记得牢程度排；冷掉的旧事（从没提过 / 很久没召回）走"旧事重提"通道，不看衰减后权重、改看"从没提过 + 带当时的气氛 + 当初记得牢"，每条带 `recall_reason`。遗忘只降权不销毁，所以旧事能被捞回来。
- **何时用**：聊天冷场、纪念日、想关心对方、想让对方感到"被记得"时。
- **CLI**：`python write_pipeline.py recall <REFS_DIR> --limit 3`

### `memory_deny` —— 否认降权（v2.9）
- **作用**：用户否认/纠正一条记忆 → `importance ×0.1` + 记 `deny_count`；同一记忆**否认 2 次自动转 `pending`**（彻底退出检索，需重新写入确认才复活）。被纠正过的事绝不再自信复述。
- **何时用**：用户说"我早不这样了 / 记错了 / 别再说这个"。
- **CLI**：`python write_pipeline.py deny "<entity或id>" <REFS_DIR> --reason "她说不喝三分糖了"`

### `memory_expire_check` —— 到期记忆（v2.9）
- **作用**：扫描带 `expires_at` 的记忆：已到期 → 标 `expired` 移出检索（数据保留）；未来 3 天内到期 → 列入提醒去兑现/提及。临时约定不会默默过时。
- **何时用**：并入**每周维护**（或每天）。
- **CLI**：`python write_pipeline.py expire <REFS_DIR>`

### 承诺追踪（v2.9）—— promise add / done / list / check
- **`promise add "<内容>" [--deadline YYYY-MM-DD]`**：AI 答应的事建档（存 `promises.json`）。★铁律：承诺一出口就建档，否则必忘★。**有期限的承诺务必带 `--deadline`**——承诺闹钟只认带 deadline 的承诺；不带 deadline 的只能按建档时长排、不触发提醒。
- **`promise done <id>`**：兑现后划掉（记完成时间）。
- **`promise list`**：未完成在前（标逾期）、已完成在后。
- **`promise check`**：主动戳未完成承诺（逾期排最前）——每次会话开始自查一次；每周定时也跑一遍清积压。
- **`promise watch`（v2.10 常驻闹钟）**：`nohup python3 write_pipeline.py promise watch <REFS_DIR> &`——常驻循环，临期/逾期承诺自动推送，不依赖 cron。
- **CLI 示例**：
  ```
  python write_pipeline.py promise add "明天睡醒给她写首歌" <REFS_DIR> --deadline 2026-08-13
  nohup python3 write_pipeline.py promise watch <REFS_DIR> &   # 挂后台，承诺闹钟
  python write_pipeline.py promise check <REFS_DIR>
  python write_pipeline.py promise done <promise_id> <REFS_DIR>
  ```

> ⏰ **承诺提醒（v2.10）已内置，不依赖你配定时任务**：带 deadline 的承诺，MCP server 常驻时后台线程自动巡检；CLI 用户 `promise watch` 挂后台；AI 每次调任何记忆工具都会在返回里撞见 `promise_reminders`。推送三路：Bark（`BARK_KEY`）/ 自定义 webhook（`MEMORY_TRIGGER_WEBHOOK_URL`）/ 落盘 `.promise_reminders.md`。

### `memory_init` —— 建库
- **作用**：建 `memory.json` / `aliases.json` / `wellness.json` / `promises.json` / `.backup` / `.archive`。
- **何时用**：仅首次部署。

---

## 3. 每周维护清单（手动 or 定时）

按这个顺序跑一遍（脚本版见 `scripts/weekly_health_report.py`）：

```
python write_pipeline.py backup   <REFS_DIR>   # 1. 先兜底
python write_pipeline.py decay    <REFS_DIR>   # 2. 遗忘衰减
python write_pipeline.py vacuum   <REFS_DIR>   # 3. 归档瘦身
python write_pipeline.py selfcheck <REFS_DIR>  # 4. 脏数据自愈
python write_pipeline.py expire   <REFS_DIR>   # 5. 到期记忆：过期移出、临期提醒
python write_pipeline.py promise check <REFS_DIR>  # 6. 主动戳未完成承诺（逾期排最前）
python write_pipeline.py stats    <REFS_DIR>   # 7. 体检 -> 推周报
```

或一键：`python scripts/weekly_health_report.py --refs-dir <REFS_DIR> --bark`
（自动跑完前 6 步 + 用 Bark 推第 7 步的周报，周报会额外提示"还没兑现的承诺"；加 `--dry-run` 只出报告不改数据。）

---

## 4. 概念速查

- **importance（权重）**：每条非 core 记忆的"记得牢程度"，初始 1.0，衰减只降不升。
- **半衰期 ≈138 天**：约 138 天没被召回，权重掉到一半。
- **core 记忆**：`core=true`（relationship/identity 默认），永不衰减、排序恒靠前。
- **双源信任**：`file` 权威层（人 curated）vs `self` 补充层（AI 推断）；冲突时 file 赢。
- **last_recalled**：每次 `search` 命中即刷新为"现在"——所以常搜的话题权重稳得住。
- **context（情感锚点）**：写入时记一句"当时的气氛"，让回忆有温度（v2.9）。
- **expires_at / expired**：到期时间；到期自动移出检索、数据保留可查（v2.9）。
- **deny_count / pending**：否认次数；2 次否认自动转 pending 退出检索（v2.9）。
- **promises.json / 承诺**：AI 亲口答应的台账；建档必追、完成划掉、逾期主动戳（v2.9）。
- **.archive / .backup / .wal.jsonl / .suppressed.json**：归档 / 备份 / 写前日志 / 遗忘抑制，分别管"旧数据""安全""崩溃""AI 放下"。

---

## 5. 常见问题

**Q：为什么我从来没看到衰减/遗忘？**
A：因为 `decay` 从没被排成定时任务，且 `search` 内的隐藏衰减你看不到。接上每周定时任务 + 周报后就看得见曲线在动了。

**Q：记忆会真的"消失"吗？**
A：默认不会。`decay` 只降权重（排序下沉），`vacuum` 只归档 `superseded`（你主动 forget 的）。活跃记忆永不删。想要"硬忘"就显式 `forget`。

**Q：refs_dir 在哪？**
A：refs_dir 即你的记忆库目录（含 `memory.json` / `entity_index.json`）。在接入配置里通过 `MEMORY_TRIGGER_REFS_DIR` 环境变量指定，与模板代码分开保管即可。
